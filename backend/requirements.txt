fastapi==0.110.*
uvicorn==0.29.*
pydantic==2.*
pyyaml==6.*
duckdb==1.2.*
redis==5.0.*
hiredis==3.0.*

# Phase 2 only - swap in when DATA_LAYER=athena (remove duckdb above per the
# existing 3-file swap pattern: requirements.txt / profiles.yml / db/connection.py)
# pyathena==3.*
# boto3==1.*

# dbt (run separately from the API process - see backend/dbt_project/). Unlike
# the pyathena/boto3 swap above, these are NOT mutually exclusive with each
# other - dbt-core + dbt-duckdb are needed for every local `dbt run`, and
# dbt-athena-community is needed today for deploy/buildspec.yml's existing
# `dbt run --target athena` CI step, regardless of what DATA_LAYER the API
# itself is running with. All three are installed unconditionally.
dbt-core==1.*
dbt-duckdb==1.*             # local target
dbt-athena-community==1.*   # athena target (CI/prod)
