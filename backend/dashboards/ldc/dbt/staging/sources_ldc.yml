# AUTO-GENERATED by scripts/generate_dbt_models.py from dashboards/ldc/config.yaml
# Resolves {{ source('TBD_source_database', 'TBD_ldc_case_requests') }} (used by the staging
# model) to two different tables depending on dbt target:
#   - duckdb (local dev) -> dbt/seeds/seed_ldc_raw.csv, loaded via `dbt seed`
#   - athena (CI/prod)   -> the real source.database/source.table in config.yaml,
#                          confirm with the source data team before relying on it
version: 2
sources:
  - name: TBD_source_database
    schema: "{{ 'main' if target.name == 'duckdb' else 'TBD_source_database' }}"
    tables:
      - name: TBD_ldc_case_requests
        identifier: "{{ 'seed_ldc_raw' if target.name == 'duckdb' else 'TBD_ldc_case_requests' }}"
