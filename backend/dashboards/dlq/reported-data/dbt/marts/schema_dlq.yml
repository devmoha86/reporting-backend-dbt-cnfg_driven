# AUTO-GENERATED by scripts/generate_dbt_models.py from dashboards/dlq/config.yaml,
# then HAND-EDITED: dropped the `unique` test on fnm_loan_number (kept not_null).
# True grain here is (fnm_loan_number, activity_period) - a loan has one row per
# activity period, so fnm_loan_number alone is not unique. Project decision was to
# use fnm_loan_number as primary_key anyway rather than add a dbt_utils dependency
# for a composite-unique test; this comment + the dropped test are the consequence
# of that choice. Re-running the generator will overwrite this back to
# tests: [not_null, unique] - re-apply this edit if you regenerate.
version: 2
models:
  - name: mart_dlq_reported_data
    description: "Delinquency Reported Data — one row per (fnm_loan_number, activity_period)."
    columns:
      - name: fnm_loan_number
        tests: [not_null]
      - name: servicer_loan_identifier
      - name: master_servicer_number
      - name: master_servicer_name
      - name: acting_servicer_number
      - name: acting_servicer_name
      - name: activity_period
      - name: periods_delinquent
      - name: servicer_action_type
      - name: servicer_action_type_received_dt
      - name: conditional_data_attributes_action_type
      - name: delinquency_status_type_received_date
      - name: delinquency_status_type_1
      - name: delinquency_status_type_2
      - name: delinquency_status_type_3
      - name: delinquency_status_type_4
      - name: delinquency_status_type_5
      - name: delinquency_reason_type_received_date
      - name: delinquency_reason_type_1
      - name: delinquency_reason_type_2
      - name: delinquency_reason_type_3
      - name: delinquency_reason_type_4
      - name: delinquency_reason_type_5
